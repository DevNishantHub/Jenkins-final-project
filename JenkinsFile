pipeline {
    agent maven-slave
    parameters{
        choice(name:"ENV",choices:["dev","test","prod"],description:"Select the environment")
    }
    stages{
        stage('checkout'){
            agent {label 'master'}
            steps{
                git url : 
            }
        }
    }
}